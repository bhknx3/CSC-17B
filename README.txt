All projects located in this folder connect to localhost as root with no password.
This can be changed in the config.php file for the storefront and survey engine.

The included SQL files will populate a schema and its tables which contain preloaded information.
These database tables are required for the website to function properly.